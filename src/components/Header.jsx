import React from 'react';
import Navbar from './Navbar';
import CartIcon from './CartIcon';

const Header = () => {
    return (
        <header>
            <Navbar />
            <CartIcon />
        </header>
    );
}

export default Header;